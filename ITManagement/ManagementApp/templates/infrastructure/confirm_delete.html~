{% extends 'index.html' %}

{% block content %}
 <div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h1 class="mb-4">Supprimer infrastructure</h1>
      <p class="mb-4">Êtes-vous sûr de vouloir supprimer l'infrastructure "{{ infrastructure.nom }}" ?</p>
      <form method="post">
        {% csrf_token %}
        <div class="d-grid gap-2">
          <button type="submit" class="btn btn-danger">Confirmer</button>
          <button type="submit" formaction="{% url 'infrastructure_list' %}" class="btn btn-secondary">Annuler</button>
        </div>
      </form>
    </div>
  </div>
</div>
{% endblock %}
